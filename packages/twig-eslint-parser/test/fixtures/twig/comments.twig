{#
/**
 * @file
 * Fixture: a doc-comment that mentions Twig syntax.
 *
 * - card: the card to render. Printing {{ card }} outputs everything,
 *   {{ card.title }} outputs one part, and
 *   {{ card|without('footer') }} leaves the footer out.
 * - wrapper: markup around the card.
 *   {% if wrapper %} inside a comment must not start a tag.
 */
#}
{# single line #}{#- trimmed -#}{#~ line trimmed ~#}
{## looks like a documentation comment ##}
{{ foo # inline comment
}}
{{ foo # inline comment with }} inside
}}
{% set x = 1 # inline comment in a block
%}
{{ [
  'a', # first
  'b', # second
] }}
